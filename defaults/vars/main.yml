---
dir:
  app: "some location"

vhost:
  name: "domain" # domain name of the lapis app
  port: 80 # port for the domain to bind to

user:
  name: "name" # user who will run the systemd service of the container
